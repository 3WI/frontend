const initialState = {
    something: {
        getError: false,
        data: {
            one: 'one',
            two: 'two',
            three: 'three'
        }
    }
};

export default initialState;